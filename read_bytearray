from io import TextIOWrapper
myinput = open("filea" , "r")

def gen(f):
  myarray = bytearray()
  b = f.read(1)
  while b != "\n":
    myarray.extend(b.encode())
    b = f.read(1)
  return myarray

newarray = gen(myinput)
